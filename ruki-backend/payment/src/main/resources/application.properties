server.port=8083
spring.application.name=payment

# BASE DE DATOS
spring.datasource.url=${DB_URL:jdbc:postgresql://db.vjimgzqcvmevoqorpkfk.supabase.co:5432/postgres}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD}

# JPA e Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

spring.mvc.pathmatch.matching-strategy=ant_path_matcher

app.clients.order-service.url=${ORDER_SERVICE_URL:http://localhost:8082}
app.payments.base-url=http://137.184.109.26:8000
app.cors.allowed-origin=${FRONTEND_URL:http://localhost:5173}

server.forward-headers-strategy=framework

# STRIPE CONFIGURATION
stripe.api.key=${STRIPE_SECRET_KEY:dummy_key_para_pruebas_locales}
app.frontend.url=${FRONTEND_URL:http://localhost:5173}
stripe.webhook.secret=${STRIPE_WEBHOOK_SECRET:whsec_tu_secreto_de_prueba_aqui}

# JWT Y SEGURIDAD
jwt.secret=${JWT_SECRET:tu_secreto_super_largo_y_seguro_para_jwt_aqui_1234567890}
jwt.expiration=86400000

# JACKSON
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.date-format=yyyy-MM-dd HH:mm:ss.SSSSSS
spring.jackson.deserialization.fail-on-unknown-properties=true